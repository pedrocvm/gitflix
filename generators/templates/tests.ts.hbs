import { shallowMount } from '@vue/test-utils';
import {{pascalCase name}} from '@/components/{{type}}/{{pascalCase name}}/{{pascalCase name}}.vue';

describe('{{pascalCase name}}.vue', () => {
  it('should render correctly', () => {
    const wrapper = shallowMount({{pascalCase name}}, {
      propsData: {}
    });

    expect(wrapper.text()).toMatch('{{pascalCase name}}');
  });
});
